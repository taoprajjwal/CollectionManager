#:import os os

<Tabb>:
	do_default_tab:False

	TabbedPanelItem:
		text:'1'
		Scrool:
	TabbedPanelItem
		text:'2'
		Main_Load:
<VidWindow>:
	RelativeLayout:
		ImageButton:
			source:'/home/taoprajjwal/Desktop/pemfc.gif'
			allow_stretch:True
			keep_ratio:False
			size_hint:(1,0.9)
			on_press:root.oppen()
		Label:
			text:'203SA3kPgr56iAi'
			size_hint:(1,0.1)
			pos_hint:{'x':0,'top':1}

<Main_Load>:
	Button:
		size_hint:(0.1,0.05)
		pos_hint:{'right':1,'top':1}
		text:'Load files'
		on_release:root.open_popup()
					
<Dialog>:
	FileChooserListView:
		id: filechooser
	    dirselect:True
	    rootpath:'/'
	    show_hidden:True
# change rootpath to os.getenv("HOME")	
	BoxLayout:
		size_hint_y: None
	    height: 30
	    Button:
	    	text: "Load"
	        on_press:root.load(filechooser.selection)
	    	
<VidWindow_Load>:
	Label:
		text:root.files
		text_size:self.size	